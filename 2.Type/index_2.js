// 1. Number Type
let num = 10;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

console.log(1 * "Hello"); // 연산값 = NaN 

// 2.String Type 
let myName = "name";
let location = "위치";
let introduce = myName + location;

// 템플릿 리터럴 문법
let introduceText = `${myName}은 ${location}에 있다`;

// 3. Boolean Type
let isSwitchOn = true;
let isEmpty = false;

// 4. Null Type = 사용자가 직접 아무것도 없는 값이라고 지정 
let empty = null;

// 5. Undefined Type = 변수를 선언하고 초기화를 하지 않으면 자동으로 지정
let none; 
